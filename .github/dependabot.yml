version: 2
updates:
    -   package-ecosystem: "composer"
        directory: "/"
        schedule:
            interval: "weekly"
        open-pull-requests-limit: 5
        commit-message:
            prefix: "deps"
        labels:
            - "dependencies"
            - "dependabot"
        groups:
            all-dependencies:
                patterns:
                    - "*"
    -   package-ecosystem: "docker-compose"
        directory: "/"
        schedule:
            interval: "weekly"
        open-pull-requests-limit: 5
        commit-message:
            prefix: "deps"
        labels:
            - "dependencies"
            - "dependabot"
        groups:
            all-dependencies:
                patterns:
                    - "*"
